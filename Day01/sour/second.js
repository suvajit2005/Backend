
 function sum(a,b){
    console.log(a+b);
}

//export default sum;
module.exports = sum;
// either you create the export default sum and before that you have to create package.json and inside thee package .json file you have to write type:module
