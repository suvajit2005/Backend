const {sum, sub, mul} = require("./calculator");
//by default it fetches from the index.js file, there is no need to specify the index.js file when all the current files are in the same folder

sum(2,4);
sub(3,4);
mul(6,8);

console.log("say hello to the world");